#include "main.h"
/**
 * mul - function that multiplies two integers
 *
 * @a: input integer
 * @b: input integer
 * Return: mul value of a and b
 */
int mul(int a, int b)
{
	int mul;

	mul = a * b;
	return (mul);
}
